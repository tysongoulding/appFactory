# Template Caller Workflow for Factory Apps
# Copy this file to .github/workflows/app-[id]-[name].yml and update placeholders

name: App Pipeline Trigger

on:
  push:
    branches:
      - main
      - feature/app-**
    paths:
      - 'apps/app-[ID]-[NAME]/**' # Triggers only on changes to this app's files

jobs:
  run-app-pipeline:
    uses: ./.github/workflows/app-pipeline.yml
    with:
      app-id: "app-[ID]"
      app-name: "[NAME]"
      app-path: "apps/app-[ID]-[NAME]"
      vault-role: "app-[ID]-[NAME]-role"
    secrets:
      vault-addr: ${{ secrets.VAULT_ADDR }}
